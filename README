WordFilter is an efficient program to filter out all keywords in a paragraph.
The core in this project is FSM (finite state machine),keywords matching & state transfering is done within it.

And there are two different implenmentations using java & c++.
